namespace FinalAssignment_Algorithms.Structures
{
    internal struct Coord
    {
        public int Row;
        public int Col;


        // makes the struct for coords w rows and columns
        public Coord(int row, int col)
        {
            Row = row;
            Col = col;
        }
    }
}
